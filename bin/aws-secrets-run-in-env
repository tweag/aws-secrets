#!/bin/sh -e

# aws-secrets-run-in-env
#
# Retrieves secrets then exports into the current env, executes script

app=$1
env=$2
rel=$3
base=$4
shift 4

export `aws-secrets-get $app $env $rel $base`
exec "$@"
